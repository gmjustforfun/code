一. 编码中遇到的问题，
    1. ndaray类型的删除和增加，他的shape没有正确改变

    self.V1 = np.random.uniform(low=-v_high, high=v_high, size=(self.pop01, self.dim))

    解决方案，先用tolist变成list
    可以使用del
    在用np.array()


二 FEs指标含义
  总的适应值评估次数，不同的算法种群规模不同，所以不采用迭代次数作为衡量指标
  如果有20个粒子，总的适应值评估数为200000，则一次迭代评估20次，10000次迭代评估200000次

三 基准函数有点问题
    已经修改

四 实验结果：有的好有的差，基本上最优值都差不多，但是均值和标准差不好。算法在进化过程中陷入了局部最优解，无法跳出、

